vs code => visual studio code => install from google
github account => github.com => signup
git => version control system [download from https:it-scm.com/] -> Standalone Installer

git commands

git init => initialize git

configuration => user name and email
    email => git config --global user.email "sample@gmail.com"
    name => git config --global user.name "sample"

git remote add <remote_name> <remote_url>  Eg: git remote add origin https://github.com/mern-trainer/sahil-batch.git
git add . => add all files
git commit -m "message" => commit all files

to view remote url => git config --get remote.origin.url

git push <remote_name> <branch_name> => push to remote

git branch <branch_name> => create branch
git branch -d <branch_name> => delete branch

git switch <branch_name> => switch to branch
git checkout <branch_name> => switch to branch

git checkout -b <branch_name> => create and switch to branch

git merge <branch_name> => merge branch






